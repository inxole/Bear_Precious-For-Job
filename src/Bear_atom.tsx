import { atom } from "recoil"

export const Bear_Hovered = atom({ key: 'BH', default: false })
export const Button_Click = atom({ key: 'BC', default: false })
export const Model_in_Action = atom({ key: 'MA', default: false })